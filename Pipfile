[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Database
mysql-connector-python = ">=8.0.0"
python-dotenv = ">=1.0.0"
pymysql = ">=1.1.0"

# Data Processing
pandas = ">=2.2.0"
pyarrow = ">=14.0.1"
fastparquet = ">=2024.2.0"
numpy = ">=1.26.4"
dask = ">=2024.2.0"
numba = ">=0.58.1"

# Analysis and ML
scikit-learn = ">=1.4.0"
scipy = ">=1.12.0"

# Visualization
matplotlib = ">=3.8.2"
seaborn = ">=0.13.2"
plotly = ">=5.18.0"
missingno = ">=0.5.2"

# Excel/CSV Support
openpyxl = ">=3.1.2"
xlrd = ">=2.0.1"
csvkit = ">=1.3.0"

# Utilities
tqdm = ">=4.66.1"
requests = ">=2.31.0"
loguru = ">=0.7.2"

[dev-packages]
# Development Tools
jupyter = ">=1.0.0"
ipykernel = ">=6.29.0"
black = ">=24.1.1"
isort = ">=5.13.2"
pylint = ">=3.0.3"

[requires]
python_version = "3.11"

[scripts]
format = "black ."
lint = "pylint src/"
sort-imports = "isort ."
notebook = "jupyter notebook"
